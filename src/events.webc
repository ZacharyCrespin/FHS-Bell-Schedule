---
title: Upcoming Events
description: Foothill High School Upcoming Events
layout: main.webc
---
<p webc:if="events.upcoming.length < 1">No Upcoming Events</p>
<div webc:for="value of events.upcoming" class="box text-left">
  <template webc:type="11ty" 11ty:type="njk">
    <h2 class="h4">{{ value["event"] }}</h2>
    <p>{{ value["date"] }}{% if value["time"] %} &middot; {{ value["time"] }}</p>{% endif %}</p>
  </template>
</div>